export enum SortOrder {
  ASC = 'asc',
  DESC = 'desc',
}

export enum PostSortField {
  TITLE = 'title',
  LIKE_COUNT = 'likeCount',
  CREATED_AT = 'createdAt',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}
